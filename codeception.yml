# global codeception file to run tests from all apps
include:
    - common
    - frontend
    - backend
    - modules/rbac
    - modules/config
paths:
    log: console/runtime/logs
settings:
    colors: true
